Easy panel polish: reliable terminal, browser landing, truthful tool cards

### Improved
- The session panel's terminal now connects reliably and recovers cleanly instead of failing with "pty not found".
- Browser sessions in the panel now land on a proper starting page instead of a blank view.
- Tool cards in the session panel now show exactly what each tool did — no more misleading summaries.
