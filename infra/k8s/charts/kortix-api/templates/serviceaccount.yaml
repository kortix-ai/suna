apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kortix-api.labels" . | nindent 4 }}
  annotations:
    {{- if not .Values.serviceAccount.roleArn }}
    {{- fail "serviceAccount.roleArn is required (the app IRSA role ARN from terraform output app_irsa_role_arn)" }}
    {{- end }}
    eks.amazonaws.com/role-arn: {{ .Values.serviceAccount.roleArn | quote }}
