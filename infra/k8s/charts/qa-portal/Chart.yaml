apiVersion: v2
name: qa-portal
description: Internal QA report portal — nginx-s3-gateway streaming Allure reports straight from a private S3 bucket (IRSA), with a generated landing page. ALB Ingress at qa.kortix.com.
type: application
# Chart version; container images are pinned per-env via values (image tags).
version: 0.2.0
appVersion: "1.0.0"
