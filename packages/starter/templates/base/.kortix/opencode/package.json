{
  "name": "kortix-opencode-config",
  "private": true,
  "kortixToolAbi": 1,
  "//": "Runtime dependencies for the OpenCode config. The Parallel web search option uses the MCP client; other provider tools use fetch.",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "4.1.8"
  }
}
