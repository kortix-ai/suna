/**
 * @internal Not covered by semver. `apps/web` depends on this storage module;
 * nothing else should. It may change shape in any release.
 */
export * from '../platform/storage/managed-storage';
