/**
 * @deprecated Import from `@kortix/sdk/internal/sandbox-connection-store` instead. These
 * zustand stores are internal machinery (outside semver); this subpath still
 * works and will keep working until the next major.
 */
export * from '../internal/sandbox-connection-store';
