/**
 * @agentpress/kanvax
 *
 * Canvas system for creating and editing visual designs.
 * Supports infinite canvas with images, frames, and AI-powered editing.
 *
 * @example
 * // Import core types and utilities
 * import type { CanvasData, CanvasElement } from '@agentpress/kanvax/core';
 * import { sanitizeElements, isCanvasFile } from '@agentpress/kanvax/core';
 *
 * // Or import everything from root
 * import { sanitizeElements, type CanvasData } from '@agentpress/kanvax';
 */

// Re-export everything from core
export * from './core';
