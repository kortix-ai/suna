{
  "name": "@kortix/worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The pi-based session worker: the harness and only the harness. Compiled per (project, sha) into a single .mjs by the API's compiled-boot pipeline (apps/api/src/git-proxy/compiled-pi-runtime.ts) and booted in a minimal worker sandbox.",
  "scripts": {
    "build": "bash scripts/build.sh",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.84.3",
    "@earendil-works/pi-ai": "0.84.3",
    "ajv": "8.18.0",
    "turndown": "7.2.4",
    "typebox": "1.3.7",
    "ws": "8.21.3"
  },
  "devDependencies": {
    "@opencode-ai/sdk": "1.18.23",
    "@types/bun": "1.3.14",
    "@types/turndown": "5.0.6",
    "@types/ws": "8.18.1",
    "typescript": "5.9.3"
  }
}
