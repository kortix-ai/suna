{
  "name": "@kortix/enterprise-release",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:bundles": "bun run src/build-bundles.ts",
    "manifest": "bun run src/generate-manifest.ts",
    "bootstrap": "bun run src/bootstrap.ts",
    "publish": "bun run src/publish.ts",
    "refresh:timestamp": "bun run src/refresh-timestamp.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@kortix/cli": "workspace:*",
    "@kortix/enterprise-updater": "workspace:*",
    "@tufjs/canonical-json": "2.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.4.0"
  }
}
