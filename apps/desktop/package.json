{
  "name": "@kortix/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Kortix desktop shell — Tauri wrapper around the web app",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "icons": "tauri icon"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
