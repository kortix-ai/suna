import { ProjectScreen } from '@/components/session/ProjectScreen';

/** The project stack: home (index) and the open page or thread (view). */
export default function ProjectLayout() {
  return <ProjectScreen />;
}
