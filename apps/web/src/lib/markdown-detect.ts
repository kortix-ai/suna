// The markdown detector lives in @kortix/shared/tool-output, shared with
// mobile. It is deliberately conservative: only unambiguous syntax counts.
export { looksLikeMarkdown } from '@kortix/shared/tool-output';
