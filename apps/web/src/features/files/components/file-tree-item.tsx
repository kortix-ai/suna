/** Git status for display purposes. */
export type GitStatusType = 'added' | 'deleted' | 'modified';

/** Custom MIME type for internal drag-and-drop. */
export const DRAG_MIME = 'application/x-file-tree-path';
