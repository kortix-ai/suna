---
title: How the pieces fit
description: How projects, sessions, and change requests work together.
---

import { Card, Cards } from 'fumadocs-ui/components/card';
import { Callout } from 'fumadocs-ui/components/callout';

This page shows how a project, a session, and a change request fit together.
It also walks through what happens, step by step, when you start a session.

## The pieces

<Cards>
  <Card title="Projects" href="/docs/concepts/projects">A git repo with a manifest.</Card>
  <Card title="Sessions" href="/docs/concepts/sessions">A branch and a sandbox for one unit of work.</Card>
  <Card title="Change requests" href="/docs/concepts/change-requests">The reviewed merge back to the default branch.</Card>
  <Card title="Agents" href="/docs/concepts/agents">A markdown persona with scoped tools.</Card>
  <Card title="Models" href="/docs/concepts/models">Which model a session uses, and who pays.</Card>
  <Card title="Secrets" href="/docs/concepts/secrets">Encrypted values injected into a sandbox.</Card>
  <Card title="Triggers" href="/docs/concepts/triggers">Schedules and webhooks that start sessions.</Card>
  <Card title="Connectors" href="/docs/concepts/connectors">Scoped reach into external apps.</Card>
  <Card title="Channels" href="/docs/concepts/channels">Chat surfaces that start sessions.</Card>
  <Card title="Computers" href="/docs/concepts/computers">Machines distinct from session sandboxes.</Card>
  <Card title="Accounts" href="/docs/concepts/accounts">Who owns projects, and who can access them.</Card>
</Cards>

## What happens when a session starts

1. Kortix creates the session row and cuts a branch from the default branch.
   The branch name is the session id.
2. Kortix resolves a sandbox image: the default image, or your own
   `.kortix/Dockerfile` if the manifest declares one.
3. The sandbox boots. Its daemon, `kortix-agent`, clones the repo to
   `/workspace` and starts OpenCode. Session status becomes `running`.
4. The agent works. It reads [secrets](/docs/concepts/secrets) as environment
   variables, then commits and pushes to the session branch.
5. The agent opens a [change request](/docs/concepts/change-requests). You
   review it and merge it — the only way work reaches the default branch.

<Callout type="info" title="Git is the only durable record">
Stopping a session pauses the sandbox but keeps its files. Deleting a session
destroys the sandbox for good. Only work committed and pushed to the branch
survives, and only a merged change request makes it permanent.
</Callout>
