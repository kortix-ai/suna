# ========================================
# SUNA AI FRONTEND - Локальная конфигурация
# ========================================
# Скопируйте этот файл в .env и заполните значения

# Режим окружения
# Используйте "local" для разработки, "staging" или "production" для продакшена
NEXT_PUBLIC_ENV_MODE="local"

# URL фронтенда (где запущен Next.js)
NEXT_PUBLIC_URL="http://localhost:3000"

# ========================================
# ВАЖНО: URL бэкенда
# ========================================
# Укажите URL, на котором работает ваш бэкенд
# Проверьте, использует ли ваш бэкенд префикс /api или нет!
# 
# Вариант 1: Бэкенд БЕЗ префикса /api
NEXT_PUBLIC_BACKEND_URL="http://localhost:8000"
#
# Вариант 2: Бэкенд С префиксом /api
# NEXT_PUBLIC_BACKEND_URL="http://localhost:8000/api"
#
# Для проверки откройте в браузере:
# http://localhost:8000/docs (FastAPI Swagger UI)

# ========================================
# SUPABASE (ОБЯЗАТЕЛЬНО!)
# ========================================
# Получите эти значения из вашего проекта Supabase:
# https://app.supabase.com/project/_/settings/api
#
NEXT_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"

# ========================================
# ОПЦИОНАЛЬНЫЕ НАСТРОЙКИ
# ========================================

# Google OAuth (если используете)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""

# PostHog Analytics (если используете)
NEXT_PUBLIC_POSTHOG_KEY=""

# Админский API ключ (должен совпадать с backend/.env)
KORTIX_ADMIN_API_KEY=""

# Edge Config (для Vercel)
EDGE_CONFIG=""

# ========================================
# ОТЛАДКА
# ========================================
# Раскомментируйте для включения дополнительного логирования
# NEXT_PUBLIC_DEBUG="true"
