# Gate 5 Release Evidence

Status: **not complete**.

This file is the prompt-to-artifact evidence log for completing `docs/SPEC.md`. Gate 5 cannot be marked complete until every required row below has current evidence from the target production-like stack.

Canonical contract:
- `docs/SPEC.md` sections 0.2, 3, 5, 7, 10, and 12 are required for production v1.
- `docs/SPEC.md` section 8 remains future scope and is not part of Gate 5.
- `docs/production-runbook.md` is the required rehearsal and rollback companion.

## SPEC Coverage Checklist

| SPEC source | Requirement | Evidence status |
|---|---|---|
| §0.2 Gate 1 | Repo-first tree cleanup, no active legacy `/instances` product surface, typechecks pass. | Passed locally: project/web typechecks, project routes, workspace cleanup, and v1 legacy script/web-route guards are green. Final target still blocked by production-like evidence rows below. |
| §0.2 Gate 2 | Sandbox image builds; daemon rejects missing/invalid HMAC user context; session branch and sandbox reach `/kortix/health`; chat opens without legacy redirects. | Passed locally/self-host: sandbox image build, daemon auth tests, local golden daemon health, and web session route test passed. Target Daytona run pending. |
| §0.2 Gate 3 | Accounts, invites, project roles, project/session access, and cross-account denial work for real teams. | Passed locally/self-host: API contract tests plus Playwright account/project access and invite auto-claim passed. Target run pending. |
| §0.2 Gate 4 | Project secrets stay cloud-side; LLM/router tokens are session-scoped; GitHub App is production repo path; branch GC and idle hibernation exist. | Passed locally for secrets/router, GitHub App code/tests, branch GC, and hibernation. Target GitHub App installation and cloud provider proof pending. |
| §0.2 Gate 5 | Billing, audit, usage, rate limits, observability, migration tooling, provider recovery, and support/admin tooling are production-ready. | Implemented and locally tested where possible. Target managed log/OTLP proof, target golden paths, and staging runbook drills pending. |
| §3.1 | Accounts are ownership/billing units; seat/compute billing reconciles through Stripe in cloud. | Billing and account tests passed locally. Target Stripe failure drill pending. |
| §3.2 | `POST /v1/projects/create-repo` uses account GitHub App installation token in production; PAT fallback is local/self-host only. | GitHub App code/tests exist; golden target runner requires `E2E_REQUIRE_GITHUB_APP=1`. Target run pending. |
| §3.3 | Git repo is source of truth with OpenCode-native config files. | Local golden path verifies `.opencode` files after repo creation and through the sandbox proxy at `/v1/p/<external>/8000/file?path=.opencode`; `api_create_repo_starter_tests` now pins the exact 10-file starter scaffold from §10.2.2 and proves `/v1/projects/create-repo` commits it with the account GitHub App installation token before project registration. Target API curl recorder/final verifier now require project detail/files/content artifacts from the server-side repo mirror plus sandbox proxy artifacts. Target GitHub App repo proof pending. |
| §3.4 | Session invariant: `session_id == sandbox_id == branch_name`; `external_id` stays provider-owned; idle hibernation and branch GC implemented. | API contract and maintenance tests passed; local golden path verifies invariant and legacy decoupling. Target Daytona proof pending. |
| §3.5 | Sandbox daemon public surface is `GET /kortix/health`, `POST /kortix/refresh`, and OpenCode proxy; daemon validates signed context for proxy paths. | Sandbox daemon auth test passed. Target API curl recorder/final verifier now require signed `/kortix/health`, OpenCode `/app`, OpenCode file proxy, and `/kortix/refresh` artifacts. Target provider run pending. |
| §3.6 | Daytona is cloud provider; local_docker is dev/self-host provider using same sandbox image contract. | Local Docker golden path passed; Daytona target proof pending. |
| §3.7 | Project secrets remain cloud-side; runtime secrets injected only at sandbox create. | API project secret tests passed; golden target proof pending through session start. |
| §3.8 | Session-scoped LLM router hides upstream provider keys and emits usage events. | Session LLM router tests passed with usage-event coverage. |
| §3.9-§3.11 | Connectors, triggers, and channels are cloud-owned production surfaces; webhook/channel events queue under backpressure. | Project connector, trigger, channel, and golden webhook tests passed locally. Target webhook run pending. |
| §3.12-§3.15 | Agents/config use project Git files; memory v2, groups, and egress proxy remain future scope. | Future items intentionally excluded from v1; no Gate 5 evidence required. |
| §3.16 | Audit, usage, ops overview, admin ops, managed logs, and OTel traces exist. | Local audit/usage/ops/admin tests passed; structured log and OTLP exporters implemented and tested. Target sink/collector proof pending. |
| §3.17 | Account compute caps, LLM buckets, invite throttles, sandbox proxy caps, and webhook backpressure are enforced and audited. | Rate-limit tests and golden backpressure local run passed. Target run pending. |
| §5 | v1 API surface for accounts, projects, sessions, sandbox proxy, secrets, routers, triggers, audit/usage, and rate limits exists. Channels/connectors are reserved placeholders after the refactor cleanup. | API suite passed locally before this cleanup; target `api-curl-proof.json` pending. |
| §7 | Legacy migration dry-run/apply/verify/rollback exists; no active code path depends on legacy runtime tables. | Local DB-gated migration test passed; target/staging migration rollback drill pending. |
| §10.2 | API contract tests cover accounts, projects, sessions, sandbox access, proxy denial, secrets, triggers, and failure modes. Channel/connector checks were removed with the legacy API. | Passed locally via `pnpm --filter kortix-api test` before this cleanup; `api_accounts_contract_tests`, `api_projects_contract_tests`, `api_project_session_contract_tests`, `api_project_triggers_contract_tests`, `api_create_repo_starter_tests`, and `api_proxy_contract_tests` are the active named verifier checks. Target `api-curl-proof.json` pending. |
| §10.3 | Web product surface is `/projects`; project shell, sessions, files, settings, account switcher, invite flow, GitHub repo open action, and admin ops load without legacy leaks. | Local Playwright/self-host runs passed. Target admin/project browser proof pending through target rehearsal. |
| §10.5 | Golden paths E2E-1 through E2E-7 pass against production-like target. | Passed locally/self-host with PAT/local_docker; target GitHub App + Daytona run pending. |
| §10.6-§10.7 | Auth boundaries, concurrency, SLOs, and negative failure modes are verified. | Locally/self-host covered by API/rate-limit tests plus `11-production-boundaries.spec.ts` for cross-account denial, project-role demotion, invite redaction, concurrent invite acceptance, provider whitelist regression, and SLO probes. Target run must enforce SLO budgets and attach `slo-proof.json` for the full §10.6.C budget list. |
| §12 A-G | Tree cleanup, typecheck, sandbox image, daemon auth, hand E2E, API/web tests, secrets/router. | Passed locally. |
| §12 H-I | GitHub App migration, branch GC, idle hibernation. | Implemented/tested locally. Target GitHub App and cloud provider evidence pending. |
| §12 J-M | Billing, audit/usage/observability, rate limits, migration tooling. | Implemented/tested locally; target observability and staging drill evidence pending. |
| §12 N | Production E2E and runbook rehearsals. | Not complete: target rehearsal and six staging drills remain pending. |

## Current Evidence

| Requirement | Evidence | Status |
|---|---|---|
| API type safety | `pnpm --filter kortix-api typecheck` passed on 2026-05-16 after router/provider trace changes, project file listing hardening, project maintenance fixes, and local_docker provider URL fixes. | Passed locally |
| Web type safety | `pnpm --filter Kortix-Computer-Frontend exec tsc --noEmit` passed on 2026-05-16 after self-hosted E2E harness updates and the fail-closed AppProviders modal default. | Passed locally |
| API contract and unit coverage | `pnpm --filter kortix-api test` passed on 2026-05-16 after router/provider trace changes, project maintenance fixes, API image hardening, repo-first token schema migration, local_docker provider URL fixes, structured request logging, and OTLP request span exporter changes. DB-gated legacy migration rollback coverage is captured separately below. | Passed locally |
| Accounts API contract | `apps/api/src/__tests__/e2e-accounts-contract.test.ts` covers the SPEC §10.2.1 account surface: auto-created personal account rows on first `GET /v1/accounts`, team account creation with owner membership and validation errors, account detail shape, owner-only rename, unknown account `404` versus known non-member `403`, existing-user member add and duplicate `409`, pending invite create/list/resend/delete, last-owner removal and demotion protections, admin-removing-owner denial, personal-account leave denial, invite redaction for wrong recipients, full invite detail for the addressed recipient, accept wrong-email `403`, accepted invite idempotency, and expired invite `410`. The suite passed with 6 tests and 55 assertions and is now a named local Gate 5 verifier check. | Passed locally |
| Projects API contract | `apps/api/src/__tests__/e2e-projects-contract.test.ts` covers the SPEC §10.2.2 existing-repo path and repo-backed project endpoints: missing `repo_url` validation, existing repo registration/upsert without starter commits, creator manager grant, account-manager project listing, account-member listing restricted to explicit project grants, project access list/grant/revoke API with account-manager implicit manager semantics and owner revoke denial, `/detail` returning parsed config and file counts, `/files` honoring `ref` and `path`, `/files/content` missing-path `400` and UTF-8 content shape, `GET /:projectId` bumping `last_opened_at`, manager-only patch of `name`, `default_branch`, and `manifest_path`, ignored `repo_url` on patch to keep the SPEC field set closed, project archive through DELETE, archived follow-up reads returning `404`, outsider `403`, and viewer manager-operation denial. The suite passed with 6 tests and 46 assertions and is now a named local Gate 5 verifier check. | Passed locally |
| Project session and secrets contract | `apps/api/src/__tests__/e2e-project-session-contract.test.ts` covers SPEC §10.2.3 session failure modes plus project secrets: secret upsert/list/delete redaction and reserved-name rejection, session list and single-session read shapes, invalid session UUID `400`, missing session `404`, missing/pending sandbox `404`, server-managed/unknown PATCH field `400`, user-owned PATCH persistence, unknown provider rejection before branch creation, `session_id == sandbox_id == branch_name`, concurrent cap enforcement before branch/sandbox creation, and DELETE preserving the branch while stopping the session. `api_project_session_contract_tests` is now a named local Gate 5 verifier check; the latest run passed with 11 tests and 77 assertions. | Passed locally |
| Project triggers contract | `apps/api/src/__tests__/e2e-project-triggers.test.ts` covers webhook trigger create/list/read/patch/delete without echoing secrets, unsigned webhook denial before event/branch creation, signed webhook fire through normal session creation, queued webhook fires under provisioning backpressure, due cron detection, and cron backpressure queueing with `last_fired_at` advancement. `api_project_triggers_contract_tests` is now a named local Gate 5 verifier check; the latest run passed with 6 tests and 46 assertions. | Passed locally |
| Router provider trace propagation | `apps/api/src/__tests__/unit-router-provider-trace.test.ts` proves Tavily, Serper, and Anthropic-compatible upstream calls receive normalized trace headers. | Passed locally |
| Legacy router LLM trace propagation | `apps/api/src/__tests__/e2e-router.test.ts` includes `propagates normalized trace headers to OpenRouter`. | Passed locally |
| Session LLM/router trace propagation | `apps/api/src/__tests__/e2e-session-llm-router.test.ts` includes upstream trace propagation and usage-event coverage. | Passed locally |
| Sandbox preview trace propagation | `apps/api/src/__tests__/e2e-preview-proxy.test.ts` includes normalized trace forwarding, generated trace headers, and `Accept-Encoding: identity` forwarding so proxied OpenCode JSON bodies are readable by Node/browser fetch clients. | Passed locally |
| Sandbox proxy contract | `apps/api/src/__tests__/e2e-preview-proxy.test.ts` now pins the SPEC §10.2.4 proxy contract: a missing `session_sandboxes.external_id` returns `404 { error: "sandbox not found" }`; `provisioning`, `stopped`, and `error` rows return `503 { error: "sandbox not ready", status }` for authorized callers; non-members still receive `403`; active rows forward the sandbox service key plus signed `X-Kortix-User-Context`; daemon signed-context `401` is surfaced as `502`; and exhausted upstream connection failures return `502`. `api_proxy_contract_tests` is now a named local Gate 5 verifier check; the latest run passed with 34 tests and 66 assertions. | Passed locally |
| GitHub App repository auth | `apps/api/src/__tests__/e2e-github-app-projects.test.ts` proves signed app JWT creation, install URL generation, installation metadata lookup, installation-token minting, normalized trace propagation to GitHub API calls, and account installation token use for repo create plus starter commits. `api_github_app_tests` is now a required local Gate 5 verifier check, and `v1_playwright_spec_guards` proves the production golden-path spec still asserts `project.metadata.github.auth_source == "app_installation"` when `E2E_REQUIRE_GITHUB_APP=1`. | Passed locally |
| Create-repo starter scaffold contract | `apps/api/src/__tests__/e2e-create-repo-starter.test.ts` pins the SPEC §10.2.2 starter paths exactly: `README.md`, `kortix.toml`, `CONTEXT.md`, `.gitignore`, `.opencode/opencode.jsonc`, `.opencode/agents/default.md`, `.opencode/agents/reviewer.md`, `.opencode/commands/plan.md`, `.opencode/commands/test.md`, and `.opencode/skills/git-workflow/SKILL.md`. The route-level tests cover `GET/POST/DELETE /v1/projects/github/installation` metadata, then drive `POST /v1/projects/create-repo` with an account GitHub App installation and prove repo creation uses the installation token, README is upserted by SHA, all 10 starter files are committed sequentially on `main` with the same installation token, the project row stores the GitHub metadata with `auth_source: "app_installation"`, and the creator receives `manager` project role. `api_create_repo_starter_tests` is now a named local Gate 5 verifier check; the latest run passed with 3 tests and 30 assertions. | Passed locally |
| Legacy migration rollback | `TEST_DATABASE_URL="$DATABASE_URL" KORTIX_TEST_DB_CONFIRM=I_UNDERSTAND_THIS_DELETES_TEST_DATA INTERNAL_KORTIX_ENV=dev bun test src/__tests__/e2e-legacy-sandbox-migration.test.ts` passed on 2026-05-16. The test covers helper and CLI dry-run/apply/verify/rollback against fixed local DB test IDs. | Passed locally |
| Project maintenance idle hibernation | A live API log on 2026-05-16 exposed a maintenance failure where the raw SQL idle-sandbox cutoff passed a JavaScript `Date` into Postgres. `apps/api/src/projects/maintenance.ts` now serializes the cutoff as an ISO timestamp and casts it to `timestamptz`; `bun test apps/api/src/__tests__/e2e-project-maintenance.test.ts` passed with a focused regression check. | Passed locally |
| Repo-first sandbox token schema | The local golden path exposed that `kortix.api_keys.sandbox_id` still had a legacy FK to `kortix.sandboxes`, blocking session sandbox token creation without a legacy `/instances` row. `supabase/migrations/00000000000040_api_keys_repo_first_sessions.sql` now drops that FK; the isolated DB applied it and `pg_constraint` showed only `api_keys_pkey` on `kortix.api_keys`. | Passed locally |
| Project session API contract failure modes | `api_project_session_contract_tests` passed on 2026-05-16 with 11 tests and 69 assertions; see the named contract row above. | Passed locally |
| Ops observability status | `/v1/ops/overview` now returns `observability` with managed log, error tracking, structured request log, trace header, and OTLP exporter configuration status. `apps/api/src/__tests__/e2e-ops-overview.test.ts` passed on 2026-05-16. | Passed locally |
| Managed request log enrichment | `apps/api/src/index.ts` emits a structured completion log for each non-noise request through `apps/api/src/lib/logger.ts`. `apps/api/src/__tests__/unit-logger-context.test.ts` passed on 2026-05-16 and proves Better Stack log payloads include request id, trace id, parent span id, user/account/project/session fields, status, and duration. | Passed locally; target sink verification pending |
| OTLP request span exporter | `apps/api/src/lib/otel.ts` exports OTLP HTTP/JSON request spans when `OTEL_EXPORTER_OTLP_TRACES_ENDPOINT` or `OTEL_EXPORTER_OTLP_ENDPOINT` is configured. `apps/api/src/__tests__/unit-otel-exporter.test.ts` passed on 2026-05-16 and proves exported spans include normalized trace id, parent span id, service resource attributes, request/account/project/session attributes, HTTP status, and timing. | Passed locally; target collector verification pending |
| Admin ops browser flow | `./tests/node_modules/.bin/playwright test --config tests/playwright.local-dev.config.ts` passed on 2026-05-16 against local API `:8008` and web `:3000`. | Passed locally |
| Sandbox daemon auth gate | `bun test apps/kortix-sandbox-agent-server/src/__tests__/proxy-auth.test.ts` passed on 2026-05-16. The suite covers unauthenticated health, missing/malformed/bad/expired user context rejection, unconfigured token refusal, valid proxy pass-through, and signed refresh. | Passed locally |
| Sandbox image build | `docker build -f apps/sandbox/Dockerfile -t kortix/sandbox:dev .` passed on 2026-05-16 and wrote image `sha256:353d5ad681b695dbeef21ec392202ba36a88b5296a2368890a1d3817ed58bbd9`. | Passed locally |
| API self-host image build | `docker build --build-arg SERVICE=apps/api -f apps/api/Dockerfile -t kortix/kortix-api:latest .` passed on 2026-05-16 after adding `git` and `ca-certificates` to the runner image so repo-backed file listing and branch operations work in self-hosted containers. Final image: `sha256:1dd0a488e371705db7a962697192cdd74eaaa88f4c537318f215103db0e2f62c`. | Passed locally |
| Local Docker provider reachability | `apps/api/src/platform/providers/local-docker.ts` now uses `KORTIX_LOCAL_DOCKER_HOST` for published sandbox port URLs; `scripts/get-kortix.sh` sets it to `host.docker.internal` and adds an API `extra_hosts` mapping. `bun test apps/api/src/__tests__/unit-local-docker-provider.test.ts` passed. | Passed locally |
| Legacy runtime helper quarantine | `scripts/start-sandbox.sh`, `apps/api/scripts/build-snapshot.ts`, and `apps/api/scripts/apply-justavps-ssh-bridge.ts` are now fail-closed guards for repo-first v1 instead of active JustAVPS/core bootstrap helpers. `scripts/start-local.sh` and `scripts/setup-env.sh` allow/generate only `local_docker` by default. `apps/api/src/platform/services/local-sandbox-health.ts` now checks Docker plus the configured local sandbox image instead of restarting a fixed legacy host service/container. The `v1_legacy_script_guards` local verification check proves those helpers fail with explicit v1 guidance and that `scripts`/`apps/api` no longer reference `core/startup`, `core/docker`, the old raw GitHub `core` bootstrap URL, `justavps-docker`, `justavps-workload`, or `JustAVPSProvider` in active v1 helper paths. | Passed locally |
| Repo-first tree cleanup guard | The legacy `core/` tree, `packages/voice`, and `packages/kortix-ocx-registry` are absent from the filesystem. Root sandbox scripts now build `apps/sandbox/Dockerfile`; dev/release workflows build and retag `kortix/sandbox`, and the old snapshot workflow is fail-closed. The API no longer whitelists a retired provider origin in CORS, and README/release docs point at the v1 sandbox image path. `docs/SPEC.md` now records the deleted package state instead of listing deleted packages as current blockers. The `v1_tree_cleanup_guards` local verification check proves only `packages/agent-tunnel`, `packages/db`, and `packages/shared` remain, the SPEC cleanup status matches that tree, and root scripts/workflows/docs do not drift back to `core/docker`, parked packages, `kortix/computer`, or retired provider public origins. | Passed locally |
| Legacy web route cleanup | `/admin/instances` and `/debug/instances` are deleted, the admin sidebar no longer links the legacy instances route, and old `?section=instances` admin redirects land on operations. The active admin accounts drawer no longer exposes the legacy instances tab or opens `InstanceSettingsModal`; OAuth consent no longer describes JustAVPS machines; the shared connecting screen no longer imports the old instance manager, platform restart path, or `/instances` escape hatch. Project routes also disable the global old instance and settings modals, do not mount the legacy command palette, use a repo-first user menu, and route top-header account settings/project picker affordances to `/accounts` and `/projects`. Active setup, trial, home CTA, navbar, admin shell/sidebar, auth callback/actions, AAL, alert/maintenance/promo, and pricing entrypoints now send authenticated users to `/projects` or `/accounts` instead of `/dashboard` or removed subscription routes; the auth return-url sanitizer rejects legacy `/dashboard`, `/instances`, `/sessions`, and subscription targets; the public home layout no longer mounts the old new-instance modal, pricing no longer opens it, the unused session dashboard shell was removed, and logged-in share pages now mount providers without legacy sidebars or global instance/settings modals. `AppProviders` now defaults the legacy global instance/settings modals off and only the parked dashboard layout opts them back in explicitly; the old sandbox-side `/kortix/preferences/model` hydration path is gated to the legacy right-rail shell so repo-first project session pages do not call it. The `v1_legacy_web_route_guards` local verification check proves these active route/support files do not reference legacy `/instances`, JustAVPS, `InstanceSettingsModal`, `useAdminSandboxes`, `useAdminAccountSandboxes`, old platform health helpers, dashboard routes, removed subscription routes, the dashboard command palette, or the old instance modal entrypoints, and it guards the fail-closed provider default. | Passed locally |
| Self-hosted GitHub golden env propagation | `scripts/get-kortix.sh` now persists optional GitHub App/PAT config into install `.env`, and `tests/e2e/self-hosted-e2e.sh` imports GitHub env from the shell or `apps/api/.env` when `E2E_ENABLE_GOLDEN_PATHS=1`. This was verified by removing GitHub keys from `/tmp/kortix-gate5-e2e/.env` before running the browser-only suite; the script printed `Injected optional GitHub env for golden paths` and passed. | Passed locally |
| Target Gate 5 rehearsal wrapper | `tests/e2e/scripts/run-gate5-target-rehearsal.sh` is the production-like evidence entry point. Its read-only `GATE5_PREFLIGHT_ONLY=1` mode validates target env, health, `/v1/ops/overview`, response trace headers, managed logs/OTLP, and active legacy state without creating users, repos, sessions, or webhooks. The full mode requires explicit destructive-run confirmation, runs account/project access, `/admin/ops`, SPEC 10.5 golden paths, and SPEC 10.6/10.7 boundary/SLO probes, writes `playwright-report.json`, and enables `E2E_REQUIRE_GITHUB_APP=1` so `/projects/create-repo` must use GitHub App installation auth instead of the PAT fallback. It also requires `E2E_ENFORCE_SLOS=1` for target runs. The runner sends a W3C trace probe to `/health` and records the returned request id and trace id in `summary.json` for sink verification. Target summaries now include `target_rehearsal_runner: "tests/e2e/scripts/run-gate5-target-rehearsal.sh"` and `evidence_contract_version: 1`; the final verifier rejects target summaries without that current contract marker. `bash -n tests/e2e/scripts/run-gate5-target-rehearsal.sh` and the gated Playwright specs loaded locally on 2026-05-16, with destructive tests skipped unless explicitly enabled. `pnpm --dir tests run test:e2e:gate5:preflight -- --help` exits with usage before env checks, and an empty-env preflight now reports the full missing input set at once: `E2E_API_URL`, `E2E_BASE_URL`, `E2E_SUPABASE_URL`, `E2E_DATABASE_URL`, `ADMIN_TOKEN`, `SUPABASE_SERVICE_ROLE_KEY`, and a Supabase anon key. | Implemented; target run pending |
| Observability sink proof recorder | `tests/e2e/scripts/record-gate5-observability-proof.sh` records `managed-log-proof.json` and `otel-trace-proof.json` from the target rehearsal `observability_probe`. The final verifier requires the managed log proof to match the probe request id and trace id, the OTel trace proof to match the probe trace id, and every proof evidence entry to resolve to a file or HTTP(S) artifact link. `bash -n` passed, and a disposable `/tmp` fixture proved the recorder writes proof files consumed by the final verifier. | Implemented; target sink proof pending |
| Runbook drill evidence verifier | `tests/e2e/scripts/record-gate5-runbook-drill.sh` records provider failure, Stripe failure, DB migration rollback, legacy migration rollback, sandbox image rollback, and API deploy rollback drills into structured `summary.json` artifacts. `tests/e2e/scripts/record-gate5-api-curl-proof.sh` records target curl proof for `/health`, `/accounts`, `/accounts/<id>`, `/accounts/<id>/members`, `/accounts/<id>/invites`, `/projects`, `/projects/<id>`, `/projects/<id>/detail`, `/projects/<id>/files`, `/projects/<id>/files/content?path=.opencode/opencode.jsonc`, `/projects/<id>/sessions`, `/projects/<id>/sessions/<sid>`, `/projects/<id>/sessions/<sid>/sandbox`, `/p/<external>/8000/kortix/health`, `/p/<external>/8000/app`, `/p/<external>/8000/file?path=.opencode`, `/p/<external>/8000/file?path=.opencode/agents`, `POST /p/<external>/8000/kortix/refresh`, and `/ops/overview`. `tests/e2e/scripts/record-gate5-slo-proof.sh` records the §10.6.C latency budget proof. `tests/e2e/scripts/record-gate5-concurrency-proof.sh` records the §10.6.B concurrency proof. `tests/e2e/scripts/record-gate5-negative-space-proof.sh` records the §10.7 negative-space proof. `tests/e2e/scripts/record-gate5-ops-exceptions.sh` records accepted ops exceptions when the target has queued work or errored sessions/sandboxes. `tests/e2e/scripts/verify-gate5-release-evidence.sh` now also requires the local verifier's named account/project/session/triggers/proxy/create-repo checks, auth-return, GitHub App, release-verifier fixture, and Playwright-spec guards, rejects target summaries/API curl proof/drill summaries that use localhost, example, `.test`, `.invalid`, private IP, empty, or non-HTTPS target URLs by default, requires target summary `api_url`, `base_url`, and `supabase_url`, requires `api-curl-proof.json` to match the target summary API URL, and requires the target API curl artifacts to prove account detail/member/invite shapes, the single project and session shapes, the server-side repo mirror exposes the OpenCode starter config, `session_id == sandbox_id == branch_name`, `sandbox_provider/provider == "daytona"`, `external_id != session_id`, daemon health with `opencode == "ok"` and `branch == session_id`, an OpenCode `/app` HTML shell, and a successful signed daemon refresh response. It also requires every runbook drill summary to carry a real staging/target API URL plus an `ops-overview-at-record.json` snapshot with `.api.status=="ok"`, and rejects plain-HTTP proof artifact URLs by default. Synthetic fixture mode writes `status: "synthetic-complete"`, `release_eligible: false`, and `synthetic_evidence_allowed: true`; insecure public HTTP target mode writes `status: "insecure-target-complete"`, `release_eligible: false`, and `insecure_target_url_seen: true`; insecure public HTTP artifact-link mode writes `status: "insecure-evidence-complete"`, `release_eligible: false`, and `insecure_evidence_url_seen: true`. Any non-release manifest mode now also requires `GATE5_ALLOW_NON_RELEASE_MANIFEST=1`; without it, the verifier exits 1 before writing the manifest. `tests/e2e/scripts/test-gate5-release-verifier-fixtures.sh` now builds a minimal evidence fixture and proves the non-release acknowledgment gate, default HTTP artifact-link rejection, malformed account shape rejection, malformed project/session shape rejection, session-invariant rejection, malformed repo-file-content rejection, malformed daemon-health rejection, malformed `/app` proof rejection, malformed refresh-proof rejection, and acknowledged non-release manifest shapes; it is required by local Gate 5 verification. | Implemented; real target/staging evidence pending |
| Runbook drill recorder evidence validation | `tests/e2e/scripts/record-gate5-runbook-drill.sh` now refuses to write a drill summary unless `E2E_API_URL` is a real HTTPS staging/target API URL, `ADMIN_TOKEN` is present, `/ops/overview` is reachable with `.api.status=="ok"`, and every `GATE5_DRILL_EVIDENCE` entry is either an HTTP(S) artifact link, an absolute file path, or a file under the drill evidence directory. Drill summaries now carry `evidence_contract_version: 1`, and the final verifier rejects every required runbook drill summary without that marker or without a valid `ops-overview-at-record.json` snapshot. The release-verifier fixture test proves missing drill contract and missing drill ops snapshot markers fail before synthetic/non-release manifests can pass. | Passed locally |
| Gate 5 proof recorder evidence validation | `record-gate5-api-curl-proof.sh`, `record-gate5-observability-proof.sh`, `record-gate5-slo-proof.sh`, `record-gate5-concurrency-proof.sh`, `record-gate5-negative-space-proof.sh`, and `record-gate5-ops-exceptions.sh` now reject stale target rehearsal summaries unless they carry `target_rehearsal_runner: "tests/e2e/scripts/run-gate5-target-rehearsal.sh"` and `evidence_contract_version: 1`. API curl, managed log, OTel trace, SLO, concurrency, negative-space, and ops exception proof files also carry `evidence_contract_version: 1`, and the final verifier requires that marker before accepting them. The proof recorders also reject unresolved evidence entries before writing final proof files. A focused SLO negative run with `GATE5_SLO_EVIDENCE=missing-slo-proof.txt` failed. Synthetic positive runs for observability, SLO, concurrency, and negative-space proof recording passed against `/tmp/kortix-gate5-verifier-14/target`; ops-exceptions negative and positive temp runs also passed, including temp-file cleanup on failure. The release-verifier fixture test now exercises the stale-contract guard for every target proof recorder and proves the final verifier rejects every target proof file without `evidence_contract_version` before allowing synthetic/non-release manifest checks. | Passed locally |
| Evidence artifact placeholder rejection | The final verifier and every Gate 5 evidence recorder now reject placeholder HTTP evidence URLs matching `example.*`, `*.example.*`, `localhost`, or `127.*`. A focused SLO proof run with `GATE5_SLO_EVIDENCE=https://example.test/slo-dashboard` failed with `Refusing placeholder evidence URL`, while the same proof with `slo-dashboard.txt` passed against the synthetic target fixture. | Passed locally |
| Concurrency cap no-sandbox proof | `tests/e2e/scripts/record-gate5-concurrency-proof.sh` now requires `GATE5_CONCURRENCY_CAP_SANDBOX_CREATED=0`, and `tests/e2e/scripts/verify-gate5-release-evidence.sh` rejects a concurrency proof unless cap enforcement proves both `branch_created=false` and `sandbox_created=false`. Focused temp checks proved missing `GATE5_CONCURRENCY_CAP_SANDBOX_CREATED` fails, `GATE5_CONCURRENCY_CAP_SANDBOX_CREATED=1` fails, and `GATE5_CONCURRENCY_CAP_SANDBOX_CREATED=0` writes the expected proof shape. The synthetic final verifier fixture failed before the new field was added, then passed after re-recording the concurrency proof with `sandbox_created=false`, writing `/tmp/kortix-gate5-verifier-14/manifest-cap-sandbox-current-local.json`. | Passed locally |
| Removed-user proxy revocation proof | `tests/e2e/scripts/record-gate5-negative-space-proof.sh` now requires `GATE5_NEGATIVE_REMOVED_USER_PROXY_STATUS=403` and `GATE5_NEGATIVE_REMOVED_USER_PROXY_SECONDS<=5`, and the final verifier rejects a negative-space proof that lacks or fails that §10.6.A boundary. Focused temp checks proved missing removed-user variables fail, `GATE5_NEGATIVE_REMOVED_USER_PROXY_SECONDS=5.1` fails, and `4.2` writes the expected proof shape. The synthetic final verifier fixture failed before the new field was added, then passed after re-recording the negative-space proof, writing `/tmp/kortix-gate5-verifier-14/manifest-removed-user-latest-local.json`. | Passed locally |
| Playwright required-title verification | `tests/e2e/scripts/verify-gate5-release-evidence.sh` now requires exact Playwright test titles for the self-hosted and target release reports, including E2E-1 through E2E-7, account/project access, admin ops, and the §10.6/§10.7 boundary and SLO specs. It also requires every mandatory title to have non-skipped execution, except the target-only E2E-5 `local_docker` skip allowed by the explicit skip rule. A synthetic target report that listed the required files but omitted titles failed with `Missing required Playwright test title`; a report that marked E2E-7 skipped failed with `Required Playwright test did not run`; and the refreshed fixture passed only after all required titles were present and required target titles ran. | Passed locally |
| Target API curl proof recorder | `tests/e2e/scripts/record-gate5-api-curl-proof.sh` performs the runbook's required target curls with a user token and admin token, writes response artifacts, and fails unless the expected account/project/session/sandbox/external ids are present, the account detail/member/invite endpoints return the expected shapes, the single project and session endpoints return the expected active Daytona shapes, the project detail/files/content endpoints prove the server-side repo mirror can read `.opencode/opencode.jsonc`, `session_id == sandbox_id == branch_name`, the target session and sandbox both use `daytona`, `external_id` is provider-owned instead of equal to `session_id`, daemon health reports `opencode == "ok"` with `branch == session_id`, `/app` returns the OpenCode HTML shell, the proxied `.opencode` starter agent files are visible, and `POST /kortix/refresh` returns a successful signed daemon refresh payload. The recorder passed against a controlled local HTTP target with nineteen endpoints. The final verifier now requires `api-curl-proof.json`, all nineteen response artifacts, and validates the artifact bodies against the recorded account/project/session/external ids plus account shapes, single project/session shapes, project repo mirror files, daemon health, `/app`, `.opencode/agents/default.md`, `.opencode/agents/reviewer.md`, and the refresh payload. The release-verifier fixture test proves malformed account shapes, malformed project/session shapes, a project-session branch mismatch, a sandbox `external_id == session_id` regression, malformed repo file content, malformed daemon health, malformed `/app` proof, and a malformed refresh proof are rejected. | Implemented; target curl proof pending |
| Production runbook proof capture | `docs/production-runbook.md` now includes explicit target commands for `test:e2e:gate5:record-slo`, `test:e2e:gate5:record-concurrency`, and `test:e2e:gate5:record-negative`, and the target rehearsal command shows `E2E_ENFORCE_SLOS=1`. Completion criteria now names the SLO, concurrency, and negative-space proof artifacts required by the final verifier. | Implemented; target proof still pending |
| SPEC/runbook drill contract alignment | `docs/SPEC.md` §10.8 and §12.N now name the same six staging drills as `docs/production-runbook.md` and `tests/e2e/scripts/verify-gate5-release-evidence.sh`: provider failure, Stripe failure, DB migration rollback, legacy migration rollback, sandbox image rollback, and API deploy rollback. `rg "provider failure, Stripe failure, DB migration rollback" docs/SPEC.md docs/production-runbook.md docs/gate5-release-evidence.md tests/e2e/scripts/verify-gate5-release-evidence.sh` confirmed the contract is aligned. | Passed locally |
| Local API health curl | `curl -fsS http://localhost:8008/v1/health` returned `{"status":"ok","service":"kortix-api","version":"dev","env":"local"}` on 2026-05-16. Header capture returned `X-Request-Id` and a W3C `traceparent`. | Passed locally |
| Local ops overview curl | A temporary local Supabase admin user was created, granted `super_admin`, signed in, and used for `curl -fsS http://localhost:8008/v1/ops/overview -H "Authorization: Bearer $ADMIN_TOKEN"` on 2026-05-16. Response showed API ok, queues at 0, audit events present, observability flags false in local env, and `active_legacy_sandboxes: 7`. | Passed locally; target still pending |
| Self-hosted E2E harness wiring | `tests/e2e/specs/04-auth-flow.spec.ts`, `08-accounts-project-access.spec.ts`, `09-admin-ops.spec.ts`, `10-production-golden-paths.spec.ts`, and `11-production-boundaries.spec.ts` now work with `E2E_ENV_FILE`; host-side tests receive the isolated install Supabase keys and host `DATABASE_URL`. `tests/e2e/self-hosted-e2e.sh` and `tests/e2e/scripts/run-full-self-hosted-e2e.sh` now run specs 04, 08, 09, 10, and 11 from the repo root. When `GATE5_SELF_HOSTED_EVIDENCE_DIR` is set, the self-hosted summary writes `evidence_contract_version: 1`; the final verifier requires that marker before accepting the local_docker golden-path artifact. `bash -n` passed for the self-hosted scripts. | Passed locally |
| Legacy Playwright spec cleanup | Pre-v1 specs for onboarding-to-dashboard, files-scope, account-deletion-on-instances, single-project paradigm, and board screenshots were deleted instead of kept in quarantine. `tests/README.md` documents the v1 active specs, and `tests/e2e/helpers/auth.ts` sends helper logins to `/projects`. The `v1_playwright_spec_guards` local verification check proves the legacy spec files are absent from both active specs and the old quarantine folder, and active specs/helpers/docs do not navigate to deleted auth/password, dashboard, subscription, or instances routes. | Passed locally |
| Isolated self-hosted clean install rehearsal | `KORTIX_E2E_INSTALL_DIR=/tmp/kortix-gate5-e2e E2E_OWNER_EMAIL=gate5-e2e@example.test E2E_OWNER_PASSWORD='Gate5E2ePass123!' bash tests/e2e/self-hosted-e2e.sh --skip-build` passed on 2026-05-16. Evidence: clean slate, installer completed, frontend/API/Supabase health passed, owner bootstrap passed, Playwright specs 04/08/09 passed with `6 passed`, and SPEC 10 golden-path specs produced `5 skipped` because `E2E_ENABLE_GOLDEN_PATHS` was not set. | Passed locally; golden paths still pending |
| Project file listing resilience | `GET /v1/projects/:projectId/files` and `/detail` now return an empty listing with `X-Kortix-Repo-Status: unavailable` when the configured repo cannot be cloned/listed, instead of surfacing a product-shell 500 for unreachable repos. This was validated by the self-hosted 08 account/project web flow. | Passed locally |
| Local project/admin Playwright rerun | `./tests/node_modules/.bin/playwright test --config tests/playwright.local-dev.config.ts` passed on 2026-05-16 after the E2E env-file changes. | Passed locally |
| Project sidebar GitHub, account switcher, and invite browser proof | `E2E_ENV_FILE=/tmp/kortix-gate5-e2e/.env pnpm --dir tests exec playwright test -c playwright.config.ts e2e/specs/08-accounts-project-access.spec.ts` passed on 2026-05-16 after `08-accounts-project-access.spec.ts` added sidebar proof that the `/projects` Kortix logo link, New session button, Sessions collapsible, Files and Settings buttons, and signed-in user menu email are visible; the `Open on GitHub` link is visible and points at the normalized repo URL in both expanded and collapsed sidebar states; the expanded GitHub pill opens a new tab through a stubbed GitHub route; settings-page proof is scoped to the main repository card; sidebar Settings and Files stay inside `/projects/:id/*` without exposing `/instances`, `/dashboard`, or bare `/sessions` links; account-switcher proof covers current account list, project-page account switching with no legacy redirect, account settings, create account, auto-selection, and `kortix.currentAccount` persistence; and account invite proof opens the browser Invite member modal, submits a pending invite, asserts the success toast and pending row, signs in the invited email, and verifies invite auto-claim through `/accounts`. The spec now prefers `SUPABASE_ANON_KEY` from `E2E_ENV_FILE` before falling back to repo-local `NEXT_PUBLIC_SUPABASE_ANON_KEY`, matching the self-hosted install env contract. The focused browser run used a rebuilt `kortix/kortix-frontend:latest` image (`sha256:9278a2cab6f88f01d36f8f9ada85aca185fa78bc7934227108834b5e2b86b06a`) so it covered the live project-sidebar GitHub affordance, project-page account switcher, sidebar Files/Settings navigation, account invite modal, and account-switcher cache update. | Passed locally |
| Project session chat browser proof | `E2E_DATABASE_URL=postgresql://postgres:<redacted>@localhost:13741/postgres E2E_ENABLE_GOLDEN_PATHS=1 E2E_GOLDEN_PROVIDER=local_docker E2E_ENV_FILE=/tmp/kortix-gate5-e2e/.env E2E_GOLDEN_LOCAL_DOCKER=1 E2E_GOLDEN_BACKPRESSURE=0 pnpm --dir tests exec playwright test -c playwright.config.ts e2e/specs/10-production-golden-paths.spec.ts --grep 'E2E-1|E2E-6'` passed on 2026-05-16 with 2 tests. The tightened E2E-6 now asserts the project session URL has no legacy redirects, the inline `Provisioning session` stage is visible, the live `SessionLayout` and `SessionChat` mount, the textarea is visible, the agent and model pickers are visible through stable accessible labels, the session appears in the sidebar by href, Files navigation stays under `/projects/:id/files`, clicking the sidebar session row returns to `/projects/:id/sessions/:sessionId`, reload keeps the same project session URL, and project/proxy responses do not return 4xx/5xx. `10-production-golden-paths.spec.ts` now also reads `SUPABASE_ANON_KEY` from `E2E_ENV_FILE`, matching the self-hosted env contract. The focused browser run used rebuilt frontend image `sha256:82890e910d58539d0c42c275073c7a95bea4d7e8d70d8c1dfbec0a408bfa082f`. | Passed locally; production GitHub App target pending |
| SPEC 10.5 golden path runner | `tests/e2e/specs/10-production-golden-paths.spec.ts` now codifies the production golden-path runner behind `E2E_ENABLE_GOLDEN_PATHS=1`. It creates real users/accounts/projects, calls `POST /v1/projects/create-repo`, starts and polls sessions, verifies session sandbox rows are decoupled from `kortix.sandboxes`, reaches `/v1/p/<external>/8000/kortix/health`, verifies `/v1/p/<external>/8000/file?path=.opencode` and `.opencode/agents` return starter files from the live sandbox, validates the web new-session route with concrete chat/sidebar/picker assertions, and fires signed webhooks. The `local_docker` provider proof is opt-in with `E2E_GOLDEN_LOCAL_DOCKER=1`; webhook backpressure saturation is opt-in with `E2E_GOLDEN_BACKPRESSURE=1`. On 2026-05-16, an isolated self-hosted run with local PAT fallback and `E2E_GOLDEN_PROVIDER=local_docker` passed all five tests: account filtering/invite auto-claim, GitHub repo project/session daemon health plus proxied `.opencode` starter agents, local_docker image health, web new-session route, and signed webhook trigger. | Passed locally; production GitHub App target pending |
| Self-hosted browser-only golden rehearsal | After recreating the isolated API container on the refreshed local image, `KORTIX_E2E_INSTALL_DIR=/tmp/kortix-gate5-e2e E2E_OWNER_EMAIL=gate5-e2e@example.test E2E_OWNER_PASSWORD='Gate5E2ePass123!' E2E_ENABLE_GOLDEN_PATHS=1 E2E_GOLDEN_LOCAL_DOCKER=1 E2E_GOLDEN_BACKPRESSURE=1 E2E_GOLDEN_PROVIDER=local_docker bash tests/e2e/self-hosted-e2e.sh --browser-only` passed on 2026-05-16 with `13 passed`, covering auth, account/project access, admin ops, SPEC 10.5 golden paths, and SPEC 10.6/10.7 production boundary/SLO probes through the supported script entry point. | Passed locally; production GitHub App target pending |

## Open Completion Items

These items are still required before the active goal can be marked complete:

| Requirement | Missing evidence or work |
|---|---|
| Production-like golden paths | `tests/e2e/specs/10-production-golden-paths.spec.ts` passed locally against the isolated self-hosted stack using the local/self-host PAT fallback. It must still pass through `pnpm --dir tests run test:e2e:gate5:target` against the target production-like stack with GitHub App installation auth, Daytona/cloud sandbox health, managed observability, zero unplanned active legacy sandboxes, and managed target config. |
| Self-hosted E2E rehearsal | Local isolated clean install rehearsal passed with the v1 non-golden suite, and the browser-only self-hosted script passed with structured golden artifacts after automatically importing local/self-host GitHub fallback env. Target rehearsal with production GitHub App auth, managed logs/OTLP, and staging/prod provider config is still required. |
| Runbook drills | `docs/production-runbook.md` provider failure, Stripe failure, DB migration rollback, legacy migration rollback, sandbox image rollback, and API deploy rollback drills must be rehearsed on staging with command output attached. |
| Final evidence verification | After local verification, self-hosted golden evidence, target rehearsal, observability sink proof, and all six staging drills are recorded, `GATE5_LOCAL_EVIDENCE_DIR=... GATE5_SELF_HOSTED_EVIDENCE_DIR=... GATE5_TARGET_EVIDENCE_DIR=... GATE5_DRILLS_EVIDENCE_DIR=... GATE5_RELEASE_MANIFEST=... pnpm --dir tests run test:e2e:gate5:verify-evidence` must pass and produce a release manifest with `status=="complete"` and `release_eligible==true`. |
| Managed logging | `/v1/ops/overview` exposes whether Better Stack logging is configured, and the final verifier now requires `managed-log-proof.json` matching the target rehearsal probe request id and trace id. The target stack still needs live sink evidence. |
| Full trace coverage | W3C trace headers now propagate through API, sandbox preview, GitHub, LLM, connector, search, and generic proxy paths. `/v1/ops/overview` exposes whether an OTLP endpoint is configured, and the final verifier now requires `otel-trace-proof.json` matching the target rehearsal probe trace id. The target stack still needs live collector evidence. |
| SLO evidence | `tests/e2e/scripts/record-gate5-slo-proof.sh` records target p95/median evidence for session create, Daytona sandbox activation, proxy health, LLM-router overhead, and `/projects` first paint. The final verifier requires the artifact to pass every §10.6.C threshold and attach evidence files or links. | Implemented; target SLO proof pending |
| Concurrency evidence | `tests/e2e/scripts/record-gate5-concurrency-proof.sh` records target evidence for parallel session creation, concurrent invite acceptance, sandbox-active race consistency, and cap enforcement before branch/sandbox creation. The final verifier requires every §10.6.B check to pass and attach evidence files or links. | Implemented; target concurrency proof pending |
| Negative-space evidence | `tests/e2e/scripts/record-gate5-negative-space-proof.sh` records target evidence that §10.7 forbidden regressions are absent: no `/instances`, bare `/sessions`, or `/dashboard` leaks, no legacy right rail, no justavps banner or accepted justavps session, correct member/outsider sandbox proxy outcomes, removed-user proxy revocation returns `403` within 5 seconds, no legacy sandbox/table contamination, and no session snapback/stale OpenCode session regression. | Implemented; target negative-space proof pending |
| Staging migration rollback evidence | The local DB migration rollback path passes. The same `migration:legacy-sandboxes` dry-run/apply/verify/rollback sequence must still be rehearsed on staging with command output attached to release notes. |
| Legacy release state | Local `/v1/ops/overview` on 2026-05-16 showed `active_legacy_sandboxes: 7`. The release target must show no unplanned active legacy sandboxes, or every remaining legacy sandbox must be tied to an approved migration window. |

## Evidence Capture Checklist

For the final Gate 5 completion audit, paste the exact command output or artifact link for each item:

| Item | Required command or artifact | Evidence |
|---|---|---|
| API health | `curl -fsS "$API_URL/v1/health"` | Pending |
| Ops overview | `curl -fsS "$API_URL/v1/ops/overview" -H "Authorization: Bearer $ADMIN_TOKEN"` | Pending |
| API typecheck | `pnpm --filter kortix-api typecheck` | Passed locally on 2026-05-16 after structured request log and Gate 5 evidence verifier changes |
| Web typecheck | `pnpm --filter Kortix-Computer-Frontend exec tsc --noEmit` | Passed locally on 2026-05-16 |
| API tests | `pnpm --filter kortix-api test` | Passed locally on 2026-05-16 after local_docker, installer, API image, project maintenance, repo-first schema, structured request logging, and OTLP span exporter fixes |
| Sandbox daemon tests | `bun test apps/kortix-sandbox-agent-server/src/__tests__/proxy-auth.test.ts` | Passed locally on 2026-05-16 |
| Sandbox image | `docker build -f apps/sandbox/Dockerfile -t kortix/sandbox:dev .` | Passed locally on 2026-05-16 |
| API image | `docker build --build-arg SERVICE=apps/api -f apps/api/Dockerfile -t kortix/kortix-api:latest .` | Passed locally on 2026-05-16: `sha256:1dd0a488e371705db7a962697192cdd74eaaa88f4c537318f215103db0e2f62c` |
| Project maintenance | `bun test apps/api/src/__tests__/e2e-project-maintenance.test.ts` | Passed locally on 2026-05-16 |
| Local Docker provider | `bun test apps/api/src/__tests__/unit-local-docker-provider.test.ts` | Passed locally on 2026-05-16 |
| Web E2E | `./tests/node_modules/.bin/playwright test --config tests/playwright.local-dev.config.ts` | Passed locally on 2026-05-16 |
| Self-hosted clean E2E | `KORTIX_E2E_INSTALL_DIR=/tmp/kortix-gate5-e2e E2E_OWNER_EMAIL=gate5-e2e@example.test E2E_OWNER_PASSWORD='Gate5E2ePass123!' bash tests/e2e/self-hosted-e2e.sh --skip-build` | Passed locally on 2026-05-16 before the boundary spec was added: 6 passed, 5 golden-path skips. The release evidence artifact below is the current golden run. |
| Golden paths | `E2E_ENABLE_GOLDEN_PATHS=1 E2E_GOLDEN_LOCAL_DOCKER=1 E2E_GOLDEN_BACKPRESSURE=1 E2E_GOLDEN_PROVIDER=local_docker E2E_ENV_FILE=/tmp/kortix-gate5-e2e/.env ./tests/node_modules/.bin/playwright test --config tests/playwright.config.ts tests/e2e/specs/10-production-golden-paths.spec.ts` | Passed locally on 2026-05-16: 5 passed; target GitHub App/cloud-provider pass pending |
| Browser-only self-hosted golden rehearsal | `KORTIX_E2E_INSTALL_DIR=/tmp/kortix-gate5-e2e E2E_OWNER_EMAIL=gate5-e2e@example.test E2E_OWNER_PASSWORD='Gate5E2ePass123!' E2E_ENABLE_GOLDEN_PATHS=1 E2E_GOLDEN_LOCAL_DOCKER=1 E2E_GOLDEN_BACKPRESSURE=1 E2E_GOLDEN_PROVIDER=local_docker bash tests/e2e/self-hosted-e2e.sh --browser-only` | Passed locally on 2026-05-16 after rebuilding/recreating the self-hosted frontend/API images: 13 passed |
| Self-hosted golden artifact | `GATE5_SELF_HOSTED_EVIDENCE_DIR=/Users/markokraemer/Projects/heyagi/suna/test-results/gate5-self-hosted/20260516T054512Z KORTIX_E2E_INSTALL_DIR=/tmp/kortix-gate5-e2e E2E_OWNER_EMAIL=gate5-e2e@example.test E2E_OWNER_PASSWORD='Gate5E2ePass123!' E2E_ENABLE_GOLDEN_PATHS=1 E2E_GOLDEN_LOCAL_DOCKER=1 E2E_GOLDEN_BACKPRESSURE=1 E2E_GOLDEN_PROVIDER=local_docker bash tests/e2e/self-hosted-e2e.sh --browser-only` passed on 2026-05-16 with `13 passed`; `summary.json` carries `evidence_contract_version: 1`, `playwright-report.json` reports 13 expected / 0 skipped / 0 unexpected / 0 flaky, and `playwright.log` proves auth/account/admin/golden/boundary specs passed. | Passed locally |
| Local verification artifact | `GATE5_LOCAL_ENV_FILE=/tmp/kortix-gate5-e2e/.env pnpm --dir tests run test:e2e:gate5:local` passed on 2026-05-16. Evidence: `test-results/gate5-local-verification/20260516T083101Z/summary.json` with 29 passing checks for API typecheck, web typecheck, API tests, focused billing, account contract, projects contract, project session/secrets contract, project connectors contract, session connector router contract, project triggers contract, project channels contract, rate-limit, sandbox proxy contract, audit, usage, GitHub App repo auth, create-repo starter scaffold contract, legacy migration rollback tooling, sandbox daemon auth, sandbox image build, API image build, Gate 5 script syntax, release-verifier fixture gates, web auth return-url tests, v1 Playwright spec guards, v1 legacy script guards, v1 tree cleanup guards, v1 legacy web-route guards, and `git diff --check`. | Passed locally |
| Target Gate 5 preflight | `GATE5_PREFLIGHT_ONLY=1 E2E_BASE_URL=... E2E_API_URL=... E2E_SUPABASE_URL=... E2E_DATABASE_URL=... E2E_ENV_FILE=... ADMIN_TOKEN=... E2E_GOLDEN_PROVIDER=daytona E2E_REQUIRE_GITHUB_APP=1 pnpm --dir tests run test:e2e:gate5:preflight` | Pending; read-only readiness check only, does not satisfy final verifier |
| Target Gate 5 rehearsal | `GATE5_TARGET_CONFIRM=I_UNDERSTAND_THIS_CREATES_TARGET_DATA E2E_BASE_URL=... E2E_API_URL=... E2E_SUPABASE_URL=... E2E_DATABASE_URL=... E2E_ENV_FILE=... ADMIN_TOKEN=... E2E_GOLDEN_PROVIDER=daytona E2E_REQUIRE_GITHUB_APP=1 E2E_ENFORCE_SLOS=1 pnpm --dir tests run test:e2e:gate5:target` | Pending |
| Observability sink proof | `GATE5_OBSERVABILITY_CONFIRM=I_VERIFIED_TARGET_OBSERVABILITY GATE5_TARGET_EVIDENCE_DIR=... GATE5_MANAGED_LOG_SINK=... GATE5_MANAGED_LOG_EVIDENCE=... GATE5_OTEL_TRACE_SINK=... GATE5_OTEL_TRACE_EVIDENCE=... pnpm --dir tests run test:e2e:gate5:record-observability` | Pending |
| Target API curl proof | `GATE5_API_CURL_CONFIRM=I_VERIFIED_TARGET_API_CURLS GATE5_TARGET_EVIDENCE_DIR=... GATE5_API_CURL_USER_TOKEN=... ADMIN_TOKEN=... GATE5_API_CURL_ACCOUNT_ID=... GATE5_API_CURL_PROJECT_ID=... GATE5_API_CURL_SESSION_ID=... GATE5_API_CURL_EXTERNAL_ID=... pnpm --dir tests run test:e2e:gate5:record-api-curl` | Pending |
| SLO proof | `GATE5_SLO_CONFIRM=I_VERIFIED_TARGET_SLOS GATE5_TARGET_EVIDENCE_DIR=... GATE5_SLO_SESSION_CREATE_P95_MS=... GATE5_SLO_SANDBOX_PROVIDER=daytona GATE5_SLO_SANDBOX_ACTIVE_P95_MS=... GATE5_SLO_PROXY_HEALTH_P95_MS=... GATE5_SLO_LLM_ROUTER_OVERHEAD_MEDIAN_MS=... GATE5_SLO_PROJECTS_FIRST_PAINT_P95_MS=... GATE5_SLO_EVIDENCE=... pnpm --dir tests run test:e2e:gate5:record-slo` | Pending |
| Concurrency proof | `GATE5_CONCURRENCY_CONFIRM=I_VERIFIED_TARGET_CONCURRENCY GATE5_TARGET_EVIDENCE_DIR=... GATE5_CONCURRENCY_PARALLEL_SESSION_REQUESTS=10 GATE5_CONCURRENCY_DISTINCT_SESSION_IDS=10 GATE5_CONCURRENCY_BRANCHES_PUSHED=10 GATE5_CONCURRENCY_SANDBOX_ROWS=10 GATE5_CONCURRENCY_DUPLICATE_KEY_ERRORS=0 GATE5_CONCURRENCY_INVITE_MEMBER_ROWS=1 GATE5_CONCURRENCY_INVITE_IDEMPOTENT_SEEN=1 GATE5_CONCURRENCY_SANDBOX_RACE_CONSISTENT=1 GATE5_CONCURRENCY_CAP_STATUS=429 GATE5_CONCURRENCY_CAP_BRANCH_CREATED=0 GATE5_CONCURRENCY_CAP_SANDBOX_CREATED=0 GATE5_CONCURRENCY_EVIDENCE=... pnpm --dir tests run test:e2e:gate5:record-concurrency` | Pending |
| Negative-space proof | `GATE5_NEGATIVE_CONFIRM=I_VERIFIED_TARGET_NEGATIVE_SPACE GATE5_TARGET_EVIDENCE_DIR=... GATE5_NEGATIVE_INSTANCES_URL_COUNT=0 GATE5_NEGATIVE_BARE_SESSIONS_URL_COUNT=0 GATE5_NEGATIVE_DASHBOARD_REDIRECT_COUNT=0 GATE5_NEGATIVE_RIGHT_RAIL_COUNT=0 GATE5_NEGATIVE_JUSTAVPS_BANNER_COUNT=0 GATE5_NEGATIVE_JUSTAVPS_SESSION_STATUS=400 GATE5_NEGATIVE_MEMBER_PROXY_STATUS=200 GATE5_NEGATIVE_OUTSIDER_PROXY_STATUS=403 GATE5_NEGATIVE_REMOVED_USER_PROXY_STATUS=403 GATE5_NEGATIVE_REMOVED_USER_PROXY_SECONDS=... GATE5_NEGATIVE_LEGACY_SANDBOX_ROWS=0 GATE5_NEGATIVE_LEGACY_PLATFORM_PROJECT_ROWS=0 GATE5_NEGATIVE_ACTIVE_SERVER_SNAPBACK_COUNT=0 GATE5_NEGATIVE_STALE_OPENCODE_SESSION_COUNT=0 GATE5_NEGATIVE_EVIDENCE=... pnpm --dir tests run test:e2e:gate5:record-negative` | Pending |
| Ops exceptions | `GATE5_OPS_EXCEPTIONS_CONFIRM=I_ACCEPT_TARGET_OPS_EXCEPTIONS GATE5_TARGET_EVIDENCE_DIR=... GATE5_OPS_EXCEPTION_ITEMS=... pnpm --dir tests run test:e2e:gate5:record-ops-exceptions` | Pending if target ops has queued work or errored sessions/sandboxes |
| Runbook drills | Provider, Stripe, DB migration, legacy migration, sandbox image, and API rollback evidence | Pending |
| Final evidence verifier | `GATE5_LOCAL_EVIDENCE_DIR=... GATE5_SELF_HOSTED_EVIDENCE_DIR=... GATE5_TARGET_EVIDENCE_DIR=... GATE5_DRILLS_EVIDENCE_DIR=... GATE5_RELEASE_MANIFEST=... pnpm --dir tests run test:e2e:gate5:verify-evidence` | Pending |
