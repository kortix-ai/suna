# KaaB hardening audit

Method: a 35-agent adversarial audit of every Kortix-as-a-Backend edge case
(plan `docs/KORTIX_AS_BACKEND_V1_PLAN.md` §4.1–4.11) against current `main` plus
the 6 open hardening PRs (#5259, #5267, #5271, #5272, #5273, #5275). Every
claimed gap was re-read and adversarially verified before landing here.

Live evidence (Postman-style HTTP against a running stack): the **model
fail-fast gap was confirmed empirically** — a bogus `opencode_model` and a bare
managed id both returned `201` with no validation. That gap is **fixed in this
change** (session-create now validates + normalizes `opencode_model`, returning
`400 INVALID_SESSION_MODEL`; see `apps/api/src/projects/lib/sessions.ts`). Full
current-main HTTP coverage runs via the ke2e suite in CI/staging.

Status legend: **[open-PR]** closed by merging the named PR · **[fix-here]**
addressed in this change · **[todo]** needs new code · **[doc]** documentation ·
**[defer]** tracked follow-up.

---

# KaaB edge-case audit

## 1. Verdict

**Not production-ready as a backend today — but close, and the gap is well-bounded.** The core security spine of KaaB is genuinely solid: the origin gate derives backend status only from token kind (never the request body), `secrets`/`origin_ref` are correctly backend-only, connector bindings fail closed (all-or-nothing base, cross-tenant rejection, agent-grant fold, revoked-mid-session), and the per-session secrets allowlist narrows on *both* boot and every hot-push. However, current `main` carries **four HIGH-severity defects live**: a cross-account idempotency-key collision that discloses a foreign tenant's session (fix in unmerged #5267), a `SLACK_BOT_TOKEN` withheld at boot but re-injected on turn 1 (fix in unmerged #5259), a top-level `runtime()` escape hatch that resolves its sandbox URL from process-global last-writer state and can talk to another end-user's live sandbox (**not** fixed by #5273), and a Daytona sandbox-down 400 on the final retry that silently drops a prompt (**not** fixed by #5271, despite its PR body claiming it). Merging the 6 PRs closes two of the four HIGH issues; the other two (`runtime()` URL bleed, sandbox-down 400 message loss) need new code. Ship-blocking set is therefore: merge the 6 PRs **plus** two targeted follow-up fixes. Everything else is medium/low correctness, missing metering, or docs.

## 2. Confirmed gaps/bugs NOT closed by merging the 6 PRs (actionable)

Ranked by severity. Items tagged "(PR #N incomplete)" are surfaced by a PR whose diff does not actually fix them — see §3.

| # | Edge case | Sev | What's wrong (file:line) | Fix |
|---|-----------|-----|--------------------------|-----|
| 1 | Top-level `runtime()` cross-tenant sandbox access (PR #5273 incomplete) | **High** | `runtime()` → `getActiveOpenCodeUrl()` reads module-global current-runtime (`server-store/active.ts:22`, `current-runtime.ts:62`); `ensureReady()` writes that global for **every** session unconditionally (`kortix.ts:752`). #5273 scopes the token but never re-resolves the URL, so with one shared KaaB account/PAT, `kortixA.runtime()` binds to whichever session last called `ensureReady()` — tenant B's sandbox. | In `createScopedKortix` make top-level `runtime()` throw (or drop it from the scoped surface); steer all runtime access to `session(pid,sid).runtime` (which is safe — `kortix.ts:932`). |
| 2 | Daytona sandbox-down 400 on final attempt → prompt silently dropped (PR #5271 incomplete) | **High** | Sandbox-down handling is gated `upstream.status===400 && attempt < MAX_RETRIES` (`preview.ts:825`); on the last attempt the block is skipped and the raw 400 returns via the generic passthrough (`preview.ts:856-863`), never reaching #5271's terminal `releasePromptDelivery`. Claim held 60s → client's same-key retry short-circuits to a bogus `200 {duplicate}`. `RETRY_DELAYS_MS≈4.25s` << cold boot, so realistic. | Call `releasePromptDelivery(promptDedupeKey)` on the sandbox-down 400 passthrough (provably pre-opencode), or route the exhausted 400 through `portUnreachableResponse`. Add a `forward-prompt-dedupe.test.ts` case: 400 "no IP address found" on every attempt → retry re-delivers. |
| 3 | Session-create does no model validation → not-servable/retired/typo'd model accepted, only fails at prompt time | Medium | `sessions.ts:828` `normalizeString(...)` (trim only), `:835` spreads verbatim into metadata; no `isModelServableForAccount`/`toOpencodeModelRef` anywhere in `sessions.ts`. Contract validates only `z.string().min(1)` (`api-contract/src/index.ts:326`). Contradicts plan §4.7 fail-fast. | Before insert: `toOpencodeModelRef(model)` then `isModelServableForAccount({...})`; 400 `INVALID_SESSION_MODEL`, store the normalized ref. Add ke2e route coverage. |
| 4 | Bare managed model id silently drops to opencode default | Medium | Same store-verbatim path; daemon `resolveOpencodeModel()` non-gateway branch splits on `/` and returns `undefined` for a slashless id (`main.ts:1415`) → opencode uses its baked default with no error. Non-gateway is the **default** project mode. | Same fix as #3 — `toOpencodeModelRef` re-prefixes `<id>` → `kortix/<id>` at create; reject anything still unservable. |
| 5 | Usage not readably attributed per `origin_ref` (headline KaaB promise unbacked) | Medium | `usage_events` has `session_id` but no `origin_ref` (`kortix.ts:1855-1883`); gateway records `accountId/actorUserId/projectId/sessionId` only (`hooks.ts:184-209`); `/v1/usage` is account-scoped, `group_by ∈ {model,provider,day}`, no session/origin filter (`usage.ts:61,99,120-169`). No endpoint returns per-origin_ref (or even per-session) spend. Plan §4.10 and guide:171 overpromise. | Either add `origin_ref` to `usage_events` + `group_by=origin_ref/session` on `/v1/usage`, **or** correct plan+guide to state the wrapper must keep its own `session_id→origin_ref` map and sum cost from the stream. |
| 6 | #5267 residual: honest low-entropy key collision → permanent 409; malicious key-squatting DoS | Low | Global key-only unique index kept (`kortix.ts:856`); rows never GC'd (`store.ts` has no delete; FK cascade only). A squatter pre-inserting `1`/`test`/`telegram:<victimProject>:<update_id>` durably denies others. | Land composite `(account_id, idempotency_key)` unique index via a two-phase rolling-safe migration; document clients MUST use high-entropy (UUID) keys. |
| 7 | #5275 residual: trigger/automation first boot resolves secrets as account-owner stand-in before `createdBy` rewritten to agent SA | Low | `fireGitTrigger` actor = account's first owner (`triggers.ts:781`, `actor.ts:6-17`); first boot resolves overrides as that stand-in (`sessions.ts:313`), then `createdBy` is rewritten to the SA post-create (`triggers.ts:928-933`); hot-push then resolves as SA → within-session flip. PR author explicitly deferred it. | Product decision: automation sessions should resolve per-user overrides as principal = null (shared rows only). Follow-up, not a v1 blocker. |
| 8 | §4.4 agent-grant alias-form mismatch can 403 a legitimate binding | Low | Create-time fold passes the **raw** binding key to `agentMayUseConnector` (`sessions.ts:646-647`; exact `includes()`, `agent-scope.ts:49-53`) while the call-site normalizes to public form (`db-deps.ts:719`). Binding `{kortix_email:...}` against grant `['email']` → spurious 403. Never under-restricts (fail-closed). | Wrap the key in `publicConnectorAlias(alias)` before the check (**not** `canonicalConnectorAlias` — that still 403s the common public-form grant). Add a both-alias-form test. |
| 9 | §4.9 env-scrub denylist duplicated across two hand-synced lists | Low | Boot deletes inline (`sessions.ts:333,339`); hot-push scrubs via `NEVER_IN_SANDBOX` (`sandbox-env-names.ts:6-8`). `buildSessionSandboxEnvVars` never calls `sanitizeSandboxEnv`. A future channel token added to one list re-creates the SLACK_BOT_TOKEN divergence. | Route the boot builder through `sanitizeSandboxEnv` (single source of truth); add a parity unit test asserting boot == hot-push key set for all channel secrets. |
| 10 | §4.8 create→start 404 recovery hinges on unbounded in-memory freshness Set, skipped for `initial_prompt` sessions | Low | `fresh-sessions.ts:10` is a plain Set, no TTL; `clearSessionFresh` has only test callers. `createProjectSession` marks fresh only `if (!initial_prompt)` (`sessions.ts:253`). Headless server leaks one UUID per empty session; low real impact (~100 B/entry, and awaited create-then-ensureReady doesn't hit the race). | Give the Set bounded/TTL eviction (or clear on first successful start) and mark fresh regardless of `initial_prompt`; or make backend-origin `/start` not depend on client-side freshness. |
| 11 | §2 no session-scoped short-lived browser token exists | Low | Plan §2 promises a "short-lived, session-scoped token" for direct browser streaming; only mints are account-wide and full-project PAT (`tokens.ts:60,121`); `publicShares.create` is preview/file only (`sessions.ts:168,218`). Plan labels feature "(optional)"; backend-proxied SSE works (example 09 `serve()`). | Ship `project.session(id).mintBrowserToken({expiresIn})` scoped to that session's turn-stream, **or** drop the plan's direct-streaming promise and document backend-proxied SSE as the only path. |
| 12 | §4 SDK example builds create body as `Record<string, unknown>` | Low | `examples/09-kaab-backend-wrapper.ts:152` — untyped body defeats all field validation (verified: `origin_ref:12345`/`secrets:'x'` type-error under `CreateProjectSessionInput`, vanish under `Record`). Examples are CI-typechecked docs. | Type the body as `CreateProjectSessionInput` (exported from root barrel; the example's spread compiles clean unchanged). |
| 13 | §1/§4.7 model expressed two divergent ways, no SDK bridge | Low | Create takes bare `opencode_model?: string` (`sessions.ts:90`), send takes `{providerID, modelID}` (`kortix.ts:40`); no `toOpencodeModelRef` exported. | Export a model-ref helper and/or accept structured `SessionModel` at create; type `opencode_model` with a doc-linked form. |
| 14 | §4.11 trigger/webhook cannot run as end-user X (origin_ref + connectors don't compose) | Low | Trigger create body sets only `agent_name/initial_prompt/opencode_model/metadata` (`triggers.ts:879-895`); `resolveSessionOrigin` classifies any `trigger:*` as `trigger`/`schedule` first (`session-origin.ts:71-73`) so `origin_ref`/`secrets` are 403'd. Unbuilt by design (plan §4.11 frames as future). | Add `origin_ref` + `connector_bindings` to the trigger spec and plumb them; introduce a composed origin or relax `canOverride` for backend-provisioned triggers. Until then document triggers as project-automation-only. |
| 15 | §4.10 concurrency caps are strictly account-level | Low | `countActiveProjectSessions(accountId)` has no origin_ref dimension (`sessions.ts:81-179`); one wrapper end-user can exhaust the account. Explicitly accepted for v1 (wrapper-enforced). Blast radius confined to the wrapper's own account. | Track native per-origin_ref cap in gateway pre-flight as deferred follow-up; note it's only viable once metering (#5) lands. |
| 16 | Warm-fork hot-swap would not deliver `KORTIX_SESSION_CONTEXT`/`_OPENCODE_MODEL`/`_ORIGIN_REF` (latent) | Low | `SHELL_SESSION_CREDS` omits these (`agent-env-file.ts:39-48`); no-restart hot-swap branch never re-applies the model. **Dormant** — live provision is cold-only (`session-sandbox.ts:229-232`); apps/api never sets `KORTIX_WARM_SEED`. | Before enabling any warm-fork/adopt provisioner, add these vars to the per-session delivery allowlist (and restart opencode for the model) or re-push via `/kortix/env`. Add a warm-path test. |
| 17 | Boot-path allowlist narrowing has no dedicated non-null e2e test | Low | Hot-push is e2e-tested with a real allowlist row (`integration-session-env-grants.test.ts:113-135`); `buildSessionSandboxEnvVars` against a non-null `secretsAllowlist` is never invoked by any test. Code is correct and symmetric — coverage gap, not a defect. | Add one integration case: session row with `secretsAllowlist=[X]`, call `buildSessionSandboxEnvVars`, assert only X present. |

## 3. Covered by an open PR (what each closes, what it misses)

| PR | Closes | Misses / residual |
|----|--------|-------------------|
| **#5267** cross-account idempotency 409 | **[HIGH] Fully closes** the foreign-session disclosure. Inserts `crossAccountIdempotencyResult(...)` as the **first** statement in the `claimed.existing` block (before `resultFromExistingCommand`, `engine.ts:124`), returns 409 `IDEMPOTENCY_KEY_CONFLICT` with no foreign `commandId/sessionId`. Single caller of `claimCreateSessionCommand` → every disclosure path closed; also masks the connector/secrets-conflict oracle. Rolling-deploy-safe, no migration. | Keeps the global key-only index → §2 item 6 (low-entropy honest 409 + squatting DoS). Needs composite-index follow-up. |
| **#5259** hardening | **[HIGH]** Adds `SLACK_BOT_TOKEN` to `NEVER_IN_SANDBOX` (`sandbox-env-names.ts`) → boot and hot-push scrub an identical set; closes the boot-scrub / turn-1 re-injection. **[MEDIUM]** Rewrites `ensureReady()` loop so a transient-null `/start` (5xx/408/429/network) is a retriable deadline-bounded tick instead of an instant `RUNTIME_UNAVAILABLE` throw. Also adds `examples/README.md` + GETTING-STARTED links + `INVALID_SESSION_RUNTIME_CONTEXT` error-table entry. | Leaves the two-list scrub **design** hand-synced → §2 item 9. Minor doc overstatement: null does **not** cover the create→start 404 race for `initial_prompt` sessions (fresh-set only) — not functional. |
| **#5271** prompt-dedupe release | Correctly **releases** the claim on the two certain-not-delivered exits (opencode-not-ready 503; connection-refused exhausted → terminal `portUnreachableResponse`) and **retains** on ambiguous (502/503 passthrough, timeout/abort/reset). At-most-once preserved. | **[HIGH] Misses** the final-attempt Daytona sandbox-down 400 passthrough (`preview.ts:825` gate) → §2 item 2. PR body advertises "Daytona sandbox-down 400 exhausted → release"; the diff never touches that block and has no test for it. |
| **#5272** connector default-fallback | **Correct AND complete.** Scopes the all-or-nothing gate to `source:'request'`, so an auto-email binding no longer disables project-default fallback for other unbound aliases; a genuinely caller-bound session still trips the gate (no regression). Focused regression test added. No second copy of the gate to patch. | — |
| **#5273** `.runtime` scoping | For the intended path `session(pid,sid).runtime`, correctly binds every method call/getter to the request config (branded client + recursive `runScoped` Proxy) — no cross-scope **token** leak. | **[HIGH] Misses** the top-level `runtime()` URL resolution → §2 item 1 (still resolves from global last-writer; worse, #5273 removed the prior `isConfigured()` throw so it now fails silently against the wrong sandbox). **[LOW, documented]** method return values that are lazily-connecting async iterables (e.g. `runtime.event.subscribe()`) read ambient config when iterated out of scope — the PR documents this and steers to the correctly-scoped `session.stream()`. Consider throwing on `runtime.event.*` under a scoped client. |
| **#5275** secret-principal | **[MEDIUM] Fully fixes** create/restart/open: single choke-point `secretsPrincipalUserId = sessionKaabRow?.createdBy ?? input.userId`, matching hot-push; regression test provisions as RESTARTER, asserts owner-val. | Trigger/automation first-boot residual → §2 item 7 (author-acknowledged, deferred). |

## 4. Verified-working (genuinely handled)

**Origin gate / spoofing** — origin derived only from `authType/apiKeyType/inSession` + server-set metadata, never the body (`session-origin.ts:58-83`; body.metadata spread first, server param last); backend only for SA/PAT/`apiKey`+`apiKeyType==='user'` via positive test, sandbox/null key → `user` (`session-origin.ts:75-82`); in-sandbox executor + agent-scoped tokens never backend (enforced by session-binding — the executor token carries `sessionId=sandboxId`); `origin_ref` 403 `origin_override_forbidden` for non-backend with whitespace/oversize bypasses closed (`sessions.ts:540-571`); queued-create replays captured derivation signals faithfully; backend sessions default `visibility:'private'` and resume locks the profile set to the session's own bindings/owner, never the resumer.

**Connector bindings** — all-or-nothing base semantics (any binding → unbound aliases resolve null; default fallback only with zero durable bindings, `session-connector-bindings.ts:356-425`); cross-tenant profile rejection (account+project+slug scoped, member-owner-only, masked NOT_FOUND); agent-grant fold rejects ungranted connectors 403 for every origin incl. backend; revoked-mid-session fails closed (returns from bound branch before fallback; both consumers reject `status!=='active'`).

**Secrets allowlist** — narrowing applied on both boot **and** all three hot-push fan-outs (`sessions.ts:298-309`, `sandbox-env-sync.ts:54,92`); `intersectSecretGrants` is a pure narrowing (null=passthrough, []=zero, never widens, unit-pinned); write-once immutable column, idempotent replay with differing allowlist → 409; connector-scope secrets never reach the sandbox (both resolvers see only `scope='runtime'` rows; allowlisting a connector secret → 404); backend-only gate + spoof resistance for `secrets`; session-bound enumeration narrowed so hidden identifiers don't leak.

**runtime_context** — caps enforced at contract **and** DB (64 entries / 16 KB / lowercase keys / scalar-only); credential-like **keys** regex-rejected; validated before any DB insert/provisioning; stored in a dedicated immutable table (no update path); restored as a single server-owned `KORTIX_SESSION_CONTEXT` with a merge guard and fail-closed on malformed persisted context; survives the secret hot-push (daemon `/kortix/env` never touches it).

**Idempotency / warm-pool / SDK** — same-account same-key replay returns the prior session without re-provisioning/re-billing; #5267 read-time guard closes the disclosure; #5271 release/retain logic sound for its covered exits; warm-pool/snapshot reuse carries no cross-session residual (cold boot from secret-free Dockerfile; daemon fully replaces env, emits `unset`; secret file is tmpfs + shredded); the core 4-step backend flow (PAT auth → per-user external profile → typed create overrides → live SSE) is first-class typed; #5273 token scoping for `session().runtime`; #5259 transient-null fix; documented contract/response-echo/error-table/CLI all match code.

## 5. Documentation gaps

| Doc gap | Sev | Fix |
|---------|-----|-----|
| **Model ref form** — every KaaB doc shows only `anthropic/claude-opus-4-8`; none warns a bare managed id must be `kortix/<id>` and silently drops to default (create does not normalize/validate). | **High** | Add a "Model reference form" note to guide §3: managed models → `kortix/<id>`, BYOK → `<provider>/<id>`; state create does not fail-fast (until §2 #3 lands). |
| **Idempotency-Key** — no KaaB doc mentions it, and SDK `sessions.create()` provides no way to send one, so retried creates double-create/double-charge. | **High** | Add "Idempotent retries" to the guide (stable per-attempt UUID `Idempotency-Key` header); add an SDK `sessions.create()` option to forward it; until then document the raw-HTTP form. Note global uniqueness + 409-on-collision + failed-replay-is-terminal. |
| **§4.6 secrets-principal wording** — plan says overrides "resolve as the session owner (origin_ref user)"; they actually key on Kortix `createdBy` (the wrapper's shared PAT owner) — **no per-end-user provider-login isolation**. | Medium | Correct plan/§4.6: principal is the session's `createdBy`; per-user overrides are NOT isolated per `origin_ref` end-user in v1. |
| **README front door** — README's KaaB section covers only `createScopedKortix` concurrency; no `origin_ref`/`secrets` override table, no link to the guide or example 09. | Medium | Extend README KaaB section with the full override table + guide link + example 09 pointer; refresh the examples blurb. |
| **KaaB discoverability** (GETTING-STARTED ends at 08, no examples index) | Medium | Covered by #5259 (merge it); additionally add a short "Kortix as a Backend" section to GETTING-STARTED. |
| **Reconnect on revoked profile** not documented (broker fails closed to null, no default fallback; wrapper must prompt reconnect). | Medium | Add to guide §3/§5. |
| **Browser streaming + visibility/resume** — guide only shows server-relay SSE; never states backend sessions are private-by-default or how (if) a browser streams directly. | Medium | Document server-relay as the supported path + private-by-default + profile-set-locked-at-start; or document the browser-token mint if §2 #11 ships. |
| **Double-prompt** — guide §2 creates WITH `initial_prompt` (auto-replies) then §4 calls `s.send()` on the same session → two turns/charges. | Medium | Pick one pattern (match example 09: create without `initial_prompt`, then send); note `initial_prompt` auto-replies. |
| **Model docs form/servability warning** (guide + SDK example) | Medium | Covered by #5259 partially; complete after §2 #3 lands. |
| §4.6 `canOverride` prose — plan says "only backend may set connector refs"; `BACKEND_ONLY_FIELDS` is only `{origin_ref, secrets}` — connectors are gated by ownership+grant, not origin. | Low | Correct plan §3/§4.6 wording. |
| §4.5 resolver returns a revoked profile **object** (status:`revoked`), not null; fail-closed depends on every caller checking `status!=='active'`. | Low | Correct plan/JSDoc; consider a `resolveActiveSessionConnectorProfile` wrapper. |
| §4.1 all-or-nothing binding footgun unguarded in SDK types (only in an example comment). | Low | Add the warning to `connector_bindings` JSDoc on `CreateProjectSessionInput`; expose the agent's connector set via a typed helper. |
| Guide error table omits `INVALID_SESSION_RUNTIME_CONTEXT` + runtime_context caps; runtime_context **values** not secret-screened. | Low | Covered by #5259 for the error code; add caps + "values are not secret-screened" caveat. |
| §4.8 plan gives no key-hazard guidance (global uniqueness, high-entropy requirement, failed-replay terminal). | Low | Document in the guide (pairs with the Idempotency-Key doc above). |

## 6. Recommended action order

**A. Merge now (closes 2 HIGH + 1 MEDIUM + docs, all complete):**
1. **#5267** — closes the cross-account session disclosure (HIGH). 2. **#5259** — closes `SLACK_BOT_TOKEN` re-injection (HIGH), `ensureReady` transient-null (MEDIUM), docs discoverability. 3. **#5275** — secret-principal (MEDIUM). 4. **#5272** — connector fallback (correct + complete). 5. **#5273** — merge for the token scoping (correct), but it does **not** close item 1 below.

**B. Implement now (new code — the two HIGH residuals the PRs miss):**
6. **Top-level `runtime()` throw/remove** under `createScopedKortix` (§2 item 1) — cross-tenant sandbox leak. 7. **Release the prompt claim on sandbox-down 400** (§2 item 2) — silent message loss; add the missing test.

**C. Implement next (MEDIUM correctness):**
8. **Model fail-fast at create** — `toOpencodeModelRef` + `isModelServableForAccount`, 400 `INVALID_SESSION_MODEL`, store normalized ref (closes §2 items 3 **and** 4 together; unblocks the HIGH model-ref doc gap). 9. **Per-origin_ref usage** — add the metering dimension OR correct plan+guide (§2 item 5).

**D. Docs (do alongside C — two are HIGH):**
10. Model-ref form (HIGH) and Idempotency-Key + SDK option (HIGH); then §4.6 secrets-principal wording, README/GETTING-STARTED KaaB, reconnect, browser-streaming/visibility, double-prompt, and the low doc corrections.

**E. Defer (tracked follow-ups, not v1 blockers):**
11. Composite `(account_id, idempotency_key)` index via two-phase migration (§2 item 6). 12. Trigger-on-behalf-of + per-origin_ref concurrency/cost caps (§2 items 14, 15) — gated on metering. 13. Warm-fork `SHELL_SESSION_CREDS` (§2 item 16) — **required before** any warm-fork provisioner is activated. 14. Env-scrub single-source-of-truth + boot-allowlist e2e test + SDK typed body/model helper + agent-grant alias fix (§2 items 8, 9, 12, 13, 17) — low-risk hygiene.