# Eager attachment upload implementation plan

**Goal:** Start private uploads at paste/drop/picker time and send durable references through first and running session prompts.

**Authority:** `docs/superpowers/specs/2026-09-08-eager-attachment-upload-design.md`. The September 8 user request authorizes implementation. Canonical worktree: `/Users/jay/root/kortix/suna-attachments-robustness`; branch `attachments-robustness`; draft PR #7148. No merge approval.

**Workflow:** Use subagent-driven-development, a fresh implementer per task, TDD, task review, and one final review. Read relevant repository skills before editing. Use `apply_patch`. Keep unrelated `output/playwright/` untouched. Use Node 22 via `PATH="/Users/jay/.nvm/versions/node/v22.22.3/bin:$PATH"` for pnpm. Never print secrets. Do not bump SDK versions or modify historical migrations.

### Task 1: Private upload API and durable prompt reference binding

Implement shared 50 MiB/file, 100 MiB/message, 20-file limits in `packages/shared/src/prompt-attachments.ts`. Add a project-authenticated attachment upload endpoint and deletion endpoint. Use raw file bodies with filename/mime metadata or multipart with an enforced actual-body ceiling. Return `{attachment_id, filename, mime, size, expires_at}`. Use a private bucket and server-minted paths. Reject empty/oversized files before storage writes. Compute SHA-256. Protect account/project/user ownership using existing project access and session-start capability gates. Mirror connector attachment storage failure/cleanup discipline without changing connector semantics.

Add generated Drizzle schema/migration for prompt attachment metadata and command references. Read migration and learnings skills first. Use the existing private staged-files bucket if appropriate; do not expose object paths or credentials. Reference rows bind command IDs to attachment IDs. Transactional binding must lock rows in deterministic order, validate owner/project/ready/expiry/count/aggregate size, and protect queued objects from cleanup. Extend wire part types and sanitization additively to accept `{type:'file', attachment_id, filename?, mime?}` without a URL. Canonicalize stored filename/mime from metadata. Preserve legacy parts. Bind references atomically in follow-up enqueue, first-session creation, and warm-session claim, including idempotent duplicate submissions. Return actionable 4xx errors for invalid handles, not a generic 500.

Add bounded cleanup to the existing maintenance owner. Expire unreferenced uploads after 24h; keep active or retryable command references. Storage removal must precede metadata removal, and concurrent prompt binding must not race cleanup. Do not clean historical base64 bodies. Expose an internal resolver for Task 2 that requires attachment_id plus command identity, returns canonical metadata/digest and a signed URL, and can read bytes for legacy fallback/native small images.

Write failing focused service/route/sanitizer/binding tests, including foreign/missing/expired/incomplete/duplicate handles, size/count limits, transactional rollback and idempotency, cleanup protection, and upload failure. Run migration lint and focused API tests. Add actual HTTP flows to the existing sessions flow family or route-appropriate existing flow after reading `tests/README.md` and testing skill. Keep manifest synchronized. Commit focused files and record RED/GREEN evidence plus exact endpoints/interfaces for Task 2/3.

### Task 2: Runtime materialization of staged attachments

Read the Task 1 report and implementation. Extend `prompt-attachment-materializer.ts` and engine wiring to resolve attachment identifiers under the queued command's binding. Reuse deterministic workspace targets and existing filename/error conventions. Native small images retain inline behavior inside the runtime request, but durable inbox parts stay handle-only. All other staged files become workspace references.

Add a daemon capability and authenticated file-import route for a short-lived storage URL issued by the API. Prevent arbitrary user-controlled URL imports; scope authorization to the internal runtime path, or use a trusted API-origin attachment download/redirect route authenticated with the session token. Apply HTTP/HTTPS policy appropriate to local selfhost and production, reject redirects to unrelated targets, cap bytes, verify expected byte count and SHA-256, and use temporary write plus atomic rename. Abort on timeout/error and clean partial files. Avoid logging signed URLs. Preserve the stale-daemon bounded fallback and retry ladder. If the security contract cannot safely restrict signed-URL import in the existing proxy model, use the trusted API-origin indirection and document it.

Write failing materializer, daemon download/security/size/digest/cleanup, and legacy capability tests before implementation. Run focused API/daemon suites. Commit and report exact capability and resolver contract.

### Task 3: SDK eager-upload transport and controller

Read SDK skill before all SDK edits. Expose project-scoped attachment upload/delete through the single Kortix client. Implement browser progress with cancellation and a framework-free fallback. Do not create a second client or put backend transport in app code. Use the existing authenticated backend transport conventions. Never leak tokens into query strings.

Add an SDK-owned upload controller/hook that starts when a File enters it, tracks stable local identity, progress/status/error and completed metadata, supports retry/remove/abort/dispose, rejects shared file/message limits before network, and prevents stale success after removal or project switch. Keep Send readiness synchronous so same-tick paste-plus-Enter cannot escape. Limit concurrent uploads with pending state if needed; begin eligible files immediately. Do not globally cancel uploads due to unrelated session readiness. Provide clear missing-project handling; do not silently queue until Send.

Add attachment_id to public session prompt types without removing legacy URL support. Synchronize exports and both surface snapshots. Write failing transport/controller/hook tests for attach start, progress, error/retry/remove, limits, unmount/project changes and successful no-repeat submit. Update SDK docs. Run mandatory SDK gates and include literal RED/GREEN outputs plus shippable YES/NO/NOT YET. Commit and document the public API for Task 4.

### Task 4: Composer integration and real user-contract verification

Read brand/design skills and relevant bundled Next documentation before web edits. Connect paste, drop, picker and supported draft restoration to the SDK hook/controller. No raw Kortix API fetch or upload state machine in web. Display pending/progress, success, retry and remove using existing tile primitives/tokens. Preserve HEIC thumbnail behavior without repeatedly converting on progress updates. Gate button and keyboard Send while any selected attachment is pending or failed. Removal aborts the matching upload. Preserve attachments on send failure and revoke blob URLs at the right lifecycle point.

Update `uploaded-file-refs.ts`, first-project prompt handlers, instant shell, and running session handlers to use completed attachment identifiers and never upload the selected files at Send. Preserve existing remote/runtime reference use cases. Reuse uploaded handles for user retries without duplicate byte uploads. Keep draft storage user-bound and do not persist blob/signed URLs. Persist safe completed metadata if supported without introducing a second upload state owner.

Add failing focused component/helper/controller-consumer tests. Extend existing browser journeys, not a new test harness, to assert outgoing upload begins before Send for paste/drop/picker, follows progress/failure/retry/removal, gates Send, carries handle-only create/warm/follow-up payloads and displays successful user attachments. Run real local HTTP and browser checks with persisted payload assertions, API negative checks, web lint/typecheck and brand audit. Update product test spec and relevant docs. Commit. The controller then pushes the draft PR, requests the preview and verifies final-head CI + real preview session before reporting completion pending merge approval.
